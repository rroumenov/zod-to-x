name: zod_to_x_dart_test
description: Persistent test package for zod-to-x Dart transpiler output verification.

environment:
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  json_annotation: ^4.9.0

dev_dependencies:
  build_runner: ^2.4.0
  json_serializable: ^6.8.0
  test: ^1.25.0
